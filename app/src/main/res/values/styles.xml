<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="popu_bag" parent="android:Animation" mce_bogus="1">
        <item name="android:windowEnterAnimation">@anim/push_popu_in</item>
        <item name="android:windowExitAnimation">@anim/push_popu_out</item>

    </style>
    <style name="toolbar_divider">
        <item name="android:background">#d7d2d2</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.3dp</item>
    </style>
    <style name="dialogProgress" parent="@android:style/Theme.Dialog">
        <item name="android:progressBarStyle">@style/myProgressBar</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="myProgressBar" parent="@android:style/Widget.ProgressBar.Inverse">
        <item name="android:indeterminateDrawable">@drawable/color_progress</item>
    </style>
</resources>
